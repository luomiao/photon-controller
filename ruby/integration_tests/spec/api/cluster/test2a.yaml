apiVersion: v1
kind: Pod
metadata:
  name: nginx-test-2a
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
      hostPort: 80
    volumeMounts:
    - name: test-volume-2a
      mountPath: /data/
  volumes:
  - name: test-volume-2a
    photonPersistentDisk:
        pdID: "$KUBERNETES_PDID1"
